import { TemeratureConversionPipe } from './temerature-conversion.pipe';

describe('TemeratureConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new TemeratureConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
